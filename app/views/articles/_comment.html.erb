<div>
	<p>comment posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later</p>
	<h5><%= comment.author_name%></h5>

	<p class="comment"><%= comment.body%></p>


</div>